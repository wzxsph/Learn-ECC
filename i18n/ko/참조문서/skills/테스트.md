# 테스트

본 섹션은 각 프로그래밍 언어의 테스트 전략, TDD 워크플로우 및 E2E 테스트 패턴을 다릅니다.

---

## TDD 워크플로우

### 용도 설명
TDD 워크플로우는 모든 코드 개발이 테스트驱动 개발 원칙을 따르도록 하며, 包括全面的 테스트 커버리지.

### 사용 타이밍
- 새 기능 또는 모듈 작성 시
- 버그 또는 문제 수정 시
-现有 코드 리팩토링 시
- 새 API 엔드포인트 추가 시
- 새 컴포넌트 생성 시

### 코어 개념

**1. 테스트 우선**
항상 먼저 테스트를 작성하고, 그 다음 구현하여 테스트를 통과시킵니다.

**2. 커버리지 요구**
最低 80% 커버리지 (단위 테스트 + 통합 테스트 + E2E 테스트)

**3. 테스트 유형**
- 단위 테스트: 독립 함수 및 컴포넌트
- 통합 테스트: API 엔드포인트 및 데이터베이스 작업
- E2E 테스트: 핵심 사용자 플로우 및 브라우저自动化

**4. RED-GREEN-REFACTOR 루프**
- RED: 실패할 테스트를 먼저 작성
- GREEN: 테스트를 통과하는 최소 코드 작성
- REFACTOR: 테스트가绿인 상태에서 코드 개선

### TDD流程 단계

```bash
# 1. 테스트 작성 (RED)
npm test  # 테스트가 실패해야 함

# 2. 구현 코드 (GREEN)
# 테스트를 통과하는 최소 코드 작성

# 3. 리팩토링 (REFACTOR)
# 코드 품질 개선, 테스트 유지 green

# 4. 커버리지 검증
npm run test:coverage  # 80%+ 커버리지 확인
```

### 테스트 격리 원칙

```typescript
// 오류: 테스트가 서로 의존
test('creates user', () => { /* ... */ })
test('updates same user', () => { /* depends on previous test */ })

// 올바른 것: 각 테스트가独立 설정
test('creates user', () => {
  const user = createTestUser()
  // 테스트 로직
})

test('updates user', () => {
  const user = createTestUser()
  // 업데이트 로직
})
```

---

## Python 테스트

### 용도 설명
Python 테스트 전략은 pytest, TDD 방법론, fixtures, mocking, 매개변수화 및 커버리지 요구를 사용합니다.

### 사용 타이밍
- TDD 방법론으로 새 Python 코드 작성 시
- Python 프로젝트 테스트 suites 설계 시
- Python 테스트 커버리지 리뷰 시
- 테스트 인프라 설정 시

### 코어 개념

**1. pytest Fixtures**
`@pytest.fixture`으로 재사용 가능한 테스트 데이터를 생성합니다.

**2. 매개변수화된 테스트**
`@pytest.mark.parametrize`로 다양한 입력으로 테스트를 실행합니다.

**3. Mocking**
`unittest.mock`의 `@patch`로 외부 의존성을 모의합니다.

**4. 커버리지**
`--cov`로 커버리지를 측정하며, 목표는 80%+입니다.

### 사용 예제

```python
import pytest
from unittest.mock import patch

# Fixtures
@pytest.fixture
def database():
    db = Database(":memory:")
    db.create_tables()
    yield db
    db.close()

# 매개변수화된 테스트
@pytest.mark.parametrize("input,expected", [
    ("hello", "HELLO"),
    ("world", "WORLD"),
    ("PyThOn", "PYTHON"),
])
def test_uppercase(input, expected):
    assert input.upper() == expected

# Mocking
@patch("mypackage.external_api_call")
def test_with_mock(api_call_mock):
    api_call_mock.return_value = {"status": "success"}
    result = my_function()
    assert result["status"] == "success"

# 예외 테스트
with pytest.raises(ValueError, match="invalid input"):
    raise ValueError("invalid input provided")

# 실행 명령
pytest --cov=mypackage --cov-report=html
```

---

## Go 테스트

### 용도 설명
Go 테스트 패턴은 표驱动 테스트, 하위 테스트,基准 테스트, 퍼즈 테스트 및 테스트 커버리지를 包括하며, TDD 방법 및 관용적 Go 관례를 따릅니다.

### 사용 타이밍
- 새로운 Go 함수 또는 메서드 작성 시
-现有 코드에 테스트 커버리지 추가 시
- 성능 핵심 코드용基准 테스트 생성 시
- 입력 검증용 퍼즈 테스트 구현 시
- Go 프로젝트에서 TDD 워크플로우 따르기 시

### 코어 개념

**1. 표驱动 테스트**
표驱动 패턴으로全面的 커버리지 구현.

**2. 하위 테스트**
`t.Run`으로 관련 테스트를 구성.

**3. 테스트 헬퍼 함수**
`t.Helper()`로 헬퍼 함수를 표시.

**4. 인터페이스 Mocking**
인터페이스로 의존성 解耦 및 테스트.

**5.基准 테스트 및 퍼즈 테스트**
성능 테스트 및 무작위 입력 테스트.

### 사용 예제

```go
// 표驱动 테스트
func TestAdd(t *testing.T) {
    tests := []struct {
        name     string
        a, b     int
        expected int
    }{
        {"positive numbers", 2, 3, 5},
        {"negative numbers", -1, -2, -3},
        {"zero values", 0, 0, 0},
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            got := Add(tt.a, tt.b)
            if got != tt.expected {
                t.Errorf("Add(%d, %d) = %d; want %d", tt.a, tt.b, got, tt.expected)
            }
        })
    }
}

// 테스트 헬퍼 함수
func setupTestDB(t *testing.T) *sql.DB {
    t.Helper()
    db, err := sql.Open("sqlite3", ":memory:")
    if err != nil {
        t.Fatalf("failed to open database: %v", err)
    }
    t.Cleanup(func() { db.Close() })
    return db
}

// 인터페이스 Mocking
type UserRepository interface {
    GetUser(id string) (*User, error)
    SaveUser(user *User) error
}

type MockUserRepository struct {
    GetUserFunc  func(id string) (*User, error)
    SaveUserFunc func(user *User) error
}

func (m *MockUserRepository) GetUser(id string) (*User, error) {
    return m.GetUserFunc(id)
}

// 기준 테스트
func BenchmarkProcess(b *testing.B) {
    data := generateTestData(1000)
    b.ResetTimer()
    for i := 0; i < b.N; i++ {
        Process(data)
    }
}

// 실행 명령
go test -cover ./...
go test -bench=. -benchmem
go test -fuzz=FuzzParse -fuzztime=30s
```

---

## Rust 테스트

### 용도 설명
Rust 테스트 패턴은 단위 테스트, 통합 테스트, 비동기 테스트, 속성 테스트, mocking 및 커버리지를 包括하며, TDD 방법론을 따릅니다.

### 사용 타이밍
- 새로운 Rust 함수, 메서드 또는 trait 작성 시
-现有 코드에 테스트 커버리지 추가 시
- 성능 핵심 코드용基准 테스트 생성 시
- 입력 검증용 속성 테스트 구현 시
- Rust 프로젝트에서 TDD 워크플로우 따르기 시

### 코어 개념

**1. 모듈급 테스트**
`#[cfg(test)]` 모듈에서 단위 테스트 작성.

**2. 통합 테스트**
`tests/` 디렉토리에서 통합 테스트 작성.

**3. 비동기 테스트**
`#[tokio::test]`로 비동기 테스트.

**4. 속성 테스트**
`proptest`로 기반 속성 테스트.

**5. Mocking**
`mockall`로 trait 모의.

### 사용 예제

```rust
// 모듈급 테스트
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn creates_user_with_valid_email() {
        let user = User::new("Alice", "alice@example.com").unwrap();
        assert_eq!(user.display_name(), "Alice");
    }

    #[test]
    fn rejects_invalid_email() {
        let result = User::new("Bob", "not-an-email");
        assert!(result.is_err());
    }
}

// 비동기 테스트
#[tokio::test]
async fn fetches_data_successfully() {
    let client = TestClient::new().await;
    let result = client.get("/data").await;
    assert!(result.is_ok());
}

// 속성 테스트
proptest! {
    #[test]
    fn encode_decode_roundtrip(input in ".*") {
        let encoded = encode(&input);
        let decoded = decode(&encoded).unwrap();
        prop_assert_eq!(input, decoded);
    }
}

// Mocking with mockall
use mockall::{automock, predicate::eq};

#[automock]
trait UserRepository {
    fn find_by_id(&self, id: u64) -> Option<User>;
    fn save(&self, user: &User) -> Result<(), StorageError>;
}

#[test]
fn service_returns_user_when_found() {
    let mut mock = MockUserRepository::new();
    mock.expect_find_by_id()
        .with(eq(42))
        .times(1)
        .returning(|_| Some(User { id: 42, name: "Alice".into() }));

    let service = UserService::new(Box::new(mock));
    let user = service.get_user(42).unwrap();
    assert_eq!(user.name, "Alice");
}

// 실행 명령
cargo test
cargo llvm-cov --fail-under-lines 80
cargo bench
```

---

## E2E 테스트

### 용도 설명
Playwright E2E 테스트 패턴은 안정적이고 빠르며 유지보수 가능한 E2E 테스트 suites 구축을 위한 包括적 패턴을 제공하며, 페이지 객체 모델, 설정, CI/CD 통합 및 防抖 테스트 전략을 포함합니다.

### 사용 타이밍
- E2E 테스트로 완전한 사용자 플로우 검증 시
- Playwright로 브라우저自动化 시
- CI/CD에서 E2E 테스트 설정 시
- 불안정 테스트 처리 시

### 코어 개념

**1. 페이지 객체 모델 (POM)**
페이지 상호작용을 페이지 객체 클래스에 캡슐화.

**2. 테스트 격리**
각 테스트가独立 데이터 설정.

**3. 로케이터 전략**
CSS 클래스 명ではなく 의미론적 로케이터 사용.

**4. 防抖 테스트**
불안정 테스트 식별 및 처리.

**5. 아티팩트 관리**
디버깅용 스크린샷, 비디오, 추적 로그.

### 사용 예제

```typescript
import { test, expect } from '@playwright/test'

// 페이지 객체 모델
export class ItemsPage {
  readonly page: Page
  readonly searchInput: Locator
  readonly itemCards: Locator

  constructor(page: Page) {
    this.page = page
    this.searchInput = page.locator('[data-testid="search-input"]')
    this.itemCards = page.locator('[data-testid="item-card"]')
  }

  async goto() {
    await this.page.goto('/items')
    await this.page.waitForLoadState('networkidle')
  }

  async search(query: string) {
    await this.searchInput.fill(query)
    await this.page.waitForResponse(resp => resp.url().includes('/api/search'))
  }
}

// 테스트 구조
test.describe('Item Search', () => {
  let itemsPage: ItemsPage

  test.beforeEach(async ({ page }) => {
    itemsPage = new ItemsPage(page)
    await itemsPage.goto()
  })

  test('should search by keyword', async ({ page }) => {
    await itemsPage.search('test')
    const count = await itemsPage.getItemCount()
    expect(count).toBeGreaterThan(0)
    await expect(itemsPage.itemCards.first()).toContainText(/test/i)
  })
})

// Playwright 설정
export default defineConfig({
  testDir: './tests/e2e',
  retries: process.env.CI ? 2 : 0,
  use: {
    baseURL: process.env.BASE_URL || 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
  ],
})

// 防抖 테스트
test('conditional skip', async ({ page }) => {
  test.skip(process.env.CI, 'Flaky in CI - Issue #123')
})

test('flaky: complex search', async ({ page }) => {
  test.fixme(true, 'Known flaky test - Issue #123')
})
```

---

## 관련 스킬

| 스킬 | 용도 |
|------|------|
| `coding-standards` | 인코딩 표준 및 모범 사례 |
| `error-handling` | 오류 처리 패턴 |
| `security-review` | 보안 리뷰 검사 목록 |