# 프레임워크

本일부涵盖主要Web프레임워크的개발패턴、모범 사례和코어개념。

---

## Django

### 용도설명
Django개발패턴提供了可확장、可维护的生产级Django应用程序아키텍처패턴，패키지括REST API설계、ORM모범 사례、캐시、信号和미들웨어。

### 사용 타이밍
- 빌드Django Web应用程序
- 설계Django REST Framework API
- 사용Django ORM和모델
- 설정Django프로젝트구조
- 구현캐시、信号、미들웨어

### 코어개념

**1. 프로젝트구조**
권장사용`config/`디렉토리进行설정分离。

**2. 모델설계**
사용自定义QuerySet和Manager구현可复用查询逻辑。

**3. Django REST Framework패턴**
사용직렬화器、视图集和라우팅组织API。

**4. 서비스层**
将业务逻辑从视图中分离到서비스层。

**5. 캐시전략**
사용视图级캐시、模板片段캐시和低级캐시。

### 사용예제

```python
# 모델모범 사례
class Product(models.Model):
    name = models.CharField(max_length=200)
    slug = models.SlugField(unique=True)
    price = models.DecimalField(max_digits=10, decimal_places=2)
    is_active = models.BooleanField(default=True)

    objects = ProductQuerySet.as_manager()

    class Meta:
        indexes = [
            models.Index(fields=['slug']),
            models.Index(fields=['-created_at']),
        ]

# 自定义QuerySet
class ProductQuerySet(models.QuerySet):
    def active(self):
        return self.filter(is_active=True)

    def in_stock(self):
        return self.filter(stock__gt=0)

# 서비스层
class OrderService:
    @staticmethod
    @transaction.atomic
    def create_order(user, cart: Cart) -> Order:
        order = Order.objects.create(user=user, total_price=cart.total_price)
        for item in cart.items.all():
            OrderItem.objects.create(order=order, product=item.product, quantity=item.quantity)
        cart.items.all().delete()
        return order
```

---

## Laravel

### 용도설명
Laravel개발패턴提供了可확장、可维护的生产级Laravel应用程序아키텍처패턴，패키지括라우팅、控制器、서비스层、큐、事件、캐시和API资源。

### 사용 타이밍
- 빌드Laravel Web应用程序或API
- 组织控制器、서비스和领域逻辑
- 사용Eloquent모델和关系
- 사용资源和分页설계API
- 추가큐、事件、캐시和后台작업

### 코어개념

**1. 控制器 -> 서비스 -> Actions**
保持控制器薄，将协调逻辑放在서비스中，单一용도逻辑放在actions中。

**2. 라우팅모델绑定**
사용作用域绑定保持라우팅可预测。

**3. Eloquent모델**
사용타입 힌트、casts和scopes保持领域逻辑一致。

**4. 表单요청검증**
将검증逻辑放在表单요청中，转换为DTO。

**5. API资源**
사용API资源保持응답一致。

### 사용예제

```php
// Action保持单一职责
final class CreateOrderAction
{
    public function __construct(private OrderRepository $orders) {}

    public function handle(CreateOrderData $data): Order
    {
        return $this->orders->create($data);
    }
}

// 控制器사용Action
final class OrdersController extends Controller
{
    public function __construct(private CreateOrderAction $createOrder) {}

    public function store(StoreOrderRequest $request): JsonResponse
    {
        $order = $this->createOrder->handle($request->toDto());
        return response()->json(['success' => true, 'data' => OrderResource::make($order)], 201);
    }
}

// Eloquent모델
final class Project extends Model
{
    use HasFactory;

    protected $fillable = ['name', 'owner_id', 'status'];
    protected $casts = ['status' => ProjectStatus::class, 'archived_at' => 'datetime'];

    public function scopeActive(Builder $query): Builder
    {
        return $query->whereNull('archived_at');
    }
}
```

---

## NestJS

### 용도설명
NestJS개발패턴提供了모듈化TypeScript백엔드的生产级패턴，패키지括모듈、控制器、提供者、DTO검증、守卫、拦截器和설정。

### 사용 타이밍
- 빌드NestJS API或서비스
- 组织모듈、控制器和提供者
- 추가DTO검증、守卫、拦截器或异常过滤器
- 설정环境感知설정和데이터베이스통합
- 테스트NestJS单元或HTTP端点

### 코어개념

**1. 모듈化구조**
将领域코드放在기능모듈中，跨领域关注点放在`common/`中。

**2. DTO검증**
사용`class-validator`검증각요청DTO。

**3. 依赖注入**
NestJS사용依赖注入管理依赖。

**4. 守卫和拦截器**
用于인증、권한 부여和로그기록。

**5. 异常过滤器**
集中处理오류并保持응답형식一致。

### 사용예제

```typescript
// 모듈구조
@Module({
  controllers: [UsersController],
  providers: [UsersService],
  exports: [UsersService],
})
export class UsersModule {}

// DTO검증
export class CreateUserDto {
  @IsEmail()
  email!: string;

  @IsString()
  @Length(2, 80)
  name!: string;

  @IsOptional()
  @IsEnum(UserRole)
  role?: UserRole;
}

// 控制器
@Controller('users')
export class UsersController {
  constructor(private readonly usersService: UsersService) {}

  @Get(':id')
  getById(@Param('id', ParseUUIDPipe) id: string) {
    return this.usersService.getById(id);
  }
}

// 全局검증管道
app.useGlobalPipes(
  new ValidationPipe({
    whitelist: true,
    forbidNonWhitelisted: true,
    transform: true,
  }),
);
```

---

## FastAPI

### 용도설명
FastAPI패턴提供了生产级비동기API패턴，패키지括依赖注入、Pydantic요청/응답모델、OpenAPI문서、테스트、보안和生产准备。

### 사용 타이밍
- 빌드或리뷰FastAPI应用
- 拆分라우팅、schema、依赖和데이터베이스访问
- 编写调用데이터베이스或外部서비스的비동기端点
- 추가인증、권한 부여、OpenAPI문서、테스트或배포설정

### 코어개념

**1. 应用工厂**
사용工厂패턴使테스트和worker가능사용受控설정빌드应用。

**2. Pydantic Schema**
保持요청、업데이트和응답모델分离。

**3. 依赖注入**
사용依赖注入管理요청作用域资源。

**4. 비동기端点**
비동기라우팅处理器中进行I/O操作时사용비동기库。

**5. 오류 처리**
集中化领域异常，保持응답形状稳定。

### 사용예제

```python
# 应用工厂
@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()
    yield
    await close_db()

def create_app() -> FastAPI:
    app = FastAPI(title=settings.api_title, version=settings.api_version, lifespan=lifespan)
    app.include_router(users.router, prefix="/api/v1/users", tags=["users"])
    return app

# Pydantic Schema
class UserBase(BaseModel):
    email: EmailStr
    full_name: Annotated[str, Field(min_length=1, max_length=100)]

class UserCreate(UserBase):
    password: Annotated[str, Field(min_length=12, max_length=128)]

class UserResponse(UserBase):
    model_config = ConfigDict(from_attributes=True)
    id: UUID
    created_at: datetime

# 依赖注入
async def get_current_user(
    token: str = Depends(oauth2_scheme),
    db: AsyncSession = Depends(get_db),
) -> User:
    payload = decode_token(token)
    user = await db.get(User, UUID(payload["sub"]))
    if user is None:
        raise HTTPException(status_code=401, detail="Invalid token")
    return user
```

---

## Spring Boot

### 용도설명
Spring Boot개발패턴提供了可확장、生产级서비스的아키텍처和API패턴，패키지括REST API설계、分层서비스、데이터访问、캐시和비동기处理。

### 사용 타이밍
- 사용Spring MVC或WebFlux빌드REST API
- 组织控制器 -> 서비스 -> 仓库层
- 설정Spring Data JPA、캐시或비동기处理
- 추가검증、异常处理或分页
- 설정dev/staging/production环境설정파일

### 코어개념

**1. REST API구조**
사용`@RestController`和`@RequestMapping`组织端点。

**2. 仓储패턴**
사용Spring Data JPA定义데이터访问인터페이스。

**3. 서비스层事务**
사용`@Transactional`管理事务边界。

**4. DTO和검증**
사용기록유형和Bean검증注解。

**5. 异常处理**
사용`@ControllerAdvice`集中处理异常。

### 사용예제

```java
// REST控制器
@RestController
@RequestMapping("/api/markets")
@Validated
class MarketController {
  private final MarketService marketService;

  @GetMapping
  ResponseEntity<Page<MarketResponse>> list(
      @RequestParam(defaultValue = "0") int page,
      @RequestParam(defaultValue = "20") int size) {
    Page<Market> markets = marketService.list(PageRequest.of(page, size));
    return ResponseEntity.ok(markets.map(MarketResponse::from));
  }

  @PostMapping
  ResponseEntity<MarketResponse> create(@Valid @RequestBody CreateMarketRequest request) {
    Market market = marketService.create(request);
    return ResponseEntity.status(HttpStatus.CREATED).body(MarketResponse.from(market));
  }
}

// 서비스层事务
@Service
public class MarketService {
  private final MarketRepository repo;

  @Transactional
  public Market create(CreateMarketRequest request) {
    MarketEntity entity = MarketEntity.from(request);
    return Market.from(repo.save(entity));
  }
}

// DTO和검증
public record CreateMarketRequest(
    @NotBlank @Size(max = 200) String name,
    @NotBlank @Size(max = 2000) String description,
    @NotNull @FutureOrPresent Instant endDate,
    @NotEmpty List<@NotBlank String> categories) {}

// 全局异常处理
@ControllerAdvice
class GlobalExceptionHandler {
  @ExceptionHandler(MethodArgumentNotValidException.class)
  ResponseEntity<ApiError> handleValidation(MethodArgumentNotValidException ex) {
    String message = ex.getBindingResult().getFieldErrors().stream()
        .map(e -> e.getField() + ": " + e.getDefaultMessage())
        .collect(Collectors.joining(", "));
    return ResponseEntity.badRequest().body(ApiError.validation(message));
  }
}
```

---

## 관련スキル

| 技能 | 용도 |
|------|------|
| `python-patterns` | Python惯用패턴和모범 사례 |
| `django-security` | Django보안모범 사례 |
| `fastapi-patterns` | FastAPI生产级패턴 |
| `nestjs-patterns` | NestJS모듈化TypeScript백엔드 |
| `springboot-patterns` | Spring Boot아키텍처패턴 |
