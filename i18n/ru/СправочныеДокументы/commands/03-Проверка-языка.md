# Обзор языка

 ECC Используется дляЯзыки программированияКод-ревью. 

---

## /python-review

**НазначениеОписание**: Python . проверка PEP 8 , типизация, безопасность Python использование. 

**Способ использования**:
```
/python-review
```

**Сценарий использования**:
-  Python 
-  Python 
-  Python  PR
-  Python использованиеЛучшие практики

**Категория**:

### CRITICAL (Обязательно)
- SQL/
- Безопасность eval/exec 
- Pickle Безопасность
- Жёстко закодированные учётные данные
- YAML Безопасность
- ошибка except 

### HIGH (Следует)
- типизация
- 
- 
- УправлятьУправлять
- C 
-  type()  isinstance()

### MEDIUM ()
- PEP 8 
- 
- Print 
- 
- 
-  f-strings

**проверка**:
```bash
mypy .                              # Типпроверка
ruff check .                        # Linting
black --check .                     # проверка
isort --check-only .                # 
bandit -r .                         # Безопасность
pip-audit                           # 
pytest --cov=app --cov-report=term-missing  # Тестирование
```

**проблема**:

```python
# SQL инъекция
# ошибка
query = f"SELECT * FROM users WHERE id = {user_id}"

#  - Использовать параметризованные запросы
query = "SELECT * FROM users WHERE id = %s"
cursor.execute(query, (user_id,))

# 
# ошибка
def process_items(items=[]):
    items.append("new")
    return items

# 
def process_items(items=None):
    if items is None:
        items = []
    items.append("new")
    return items

# Управлять
# ошибка
f = open("config.json")
data = f.read()
f.close()

# 
with open("config.json") as f:
    data = f.read()
```

---

## /go-review

**НазначениеОписание**: Go . проверка, параллелизмбезопасность, обработка ошибокбезопасность. 

**Способ использования**:
```
/go-review
```

**Сценарий использования**:
-  Go 
-  Go 
-  Go  PR
-  Go 

**Категория**:

### CRITICAL (Обязательно)
- SQL/
- 
- Goroutine 
- Жёстко закодированные учётные данные
- Безопасность
- ошибка

### HIGH (Следует)
- ошибка
- Panic ошибкаНазад
- Context 
- 
- Реализоватьошибка
- 

### MEDIUM ()
- 
-  godoc 
- 
- Slice 
- Тестирование

**проверка**:
```bash
go vet ./...                         # 
staticcheck ./...                    # проверка ()
golangci-lint run                   # Linting
go build -race ./...                # обнаружение
govulncheck ./...                   # Уязвимости безопасности
```

---

## /kotlin-review

**НазначениеОписание**: Kotlin . проверка, Безопасность, корутиныбезопасностьбезопасность. 

**Способ использования**:
```
/kotlin-review
```

**Сценарий использования**:
-  Kotlin 
-  Kotlin 
-  Android/KMP  PR
-  Kotlin 

**Категория**:

### CRITICAL (Обязательно)
- SQL/
- Обоснование `!!`
- ТипБезопасность
- GlobalScope  (параллелизм)
- Жёстко закодированные учётные данные
- Безопасность

### HIGH (Следует)
-  ()
- корутины
- проверка
-  `when` 
-  (>50)
-  (>4)

### MEDIUM ()
-  Kotlin (Java )
- 
- Scope 
-  Sequence
- Тип

**проверка**:
```bash
./gradlew build                      # проверка
./gradlew detekt                     # 
./gradlew ktlintCheck                # проверка
./gradlew test                       # Тестирование
```

---

## /rust-review

**НазначениеОписание**: Rust . проверкавладение, обработка ошибок, Безопасность. 

**Способ использования**:
```
/rust-review
```

**Сценарий использования**:
-  Rust 
-  Rust 
-  Rust  PR
-  Rust 

**Категория**:

### CRITICAL (Обязательно)
- проверка `unwrap()`/`expect()`
-  `// SAFETY:`  `unsafe`
-  SQL инъекция
- Проверка
- Жёстко закодированные учётные данные
- 

### HIGH (Следует)
-  `.clone()` проверка
- `String`  `&str`  `impl AsRef<str>`
-  (`std::thread::sleep`)
- Тип `Send`/`Sync` 
-  `_ =>` 
-  (>50)

### MEDIUM ()
- 
-  `with_capacity`
- Обоснование clippy Предупреждение
-  API  `///` 
-  `must_use` НазадТип `#[must_use]`

**проверка**:
```bash
cargo check                           # проверка
cargo clippy -- -D warnings            # Lints
cargo fmt --check                      # проверка
cargo test                            # Тестирование
cargo audit                           # Безопасность ()
```

---

## /cpp-review

**НазначениеОписание**: C++ . проверкапамятьБезопасность,  C++ использование, параллелизмбезопасность. 

**Способ использования**:
```
/cpp-review
```

**Сценарий использования**:
-  C++ 
-  C++ 
-  C++  PR
- проверкапамятьБезопасностьпроблема

**Категория**:

### CRITICAL (Обязательно)
-  RAII  `new`/`delete`
- 
- 
- Через `system()` 
- 
- 

### HIGH (Следует)
- 
-  `std::lock_guard` / `std::scoped_lock`
- detach Управлять
- C  `static_cast`/`dynamic_cast`
-  `const` 

### MEDIUM ()
-  ( `const&`)
-  `reserve()`
-  `using namespace std;`
- Назад `[[nodiscard]]`
- Шаблон

**проверка**:
```bash
clang-tidy --checks='*,-llvmlibc-*' src/*.cpp -- -std=c++17
cppcheck --enable=all --suppress=missingIncludeSystem src/
cmake --build build -- -Wall -Wextra -Wpedantic
```

---

## /flutter-review

**НазначениеОписание**: Flutter/Dart Код-ревью. проверка, Лучшие практики, управление состоянием, производительность, безопасность. 

**Способ использования**:
```
/flutter-review
```

**Сценарий использования**:
-  Flutter/Dart  PR  (ТестированиеЧерез)
- Реализоватьфункцияпроблема
-  Flutter 
- , управление состоянием
- 

****:

|  |  |
|------|----------|
| ,  HTTP | CRITICAL |
| , управление состоянием | CRITICAL |
| проблема,  | HIGH |
|  `dispose()`, `await`  BuildContext | HIGH |
| Dart Безопасность, ошибка/ | HIGH |
| Const ,  | HIGH |
| `build()`  | HIGH |
| ,  | MEDIUM |
| Тестирование | HIGH |
|  (l10n) | MEDIUM |

---

## /fastapi-review

**НазначениеОписание**: FastAPI . проверка, , , Pydantic , безопасность, производительностьТестирование. 

**Способ использования**:
```
/fastapi-review [Содержание]
```

****:
- App factory, , 
- Pydantic 
- , , 
-  HTTP 
- CORS, , , 
- OpenAPI 
- Тестирование

---

## Обзор языка

|  |  | проверка |  |
|------|------|----------|----------|
| `/python-review` | Python | PEP 8, типизация, SQL-инъекция | CRITICAL/HIGH/MEDIUM |
| `/go-review` | Go | параллелизмБезопасность, обработка ошибок, goroutine | CRITICAL/HIGH/MEDIUM |
| `/kotlin-review` | Kotlin | Безопасность, корутины, GlobalScope | CRITICAL/HIGH/MEDIUM |
| `/rust-review` | Rust | владение, , Безопасность | CRITICAL/HIGH/MEDIUM |
| `/cpp-review` | C++ | памятьБезопасность, RAII, параллелизм | CRITICAL/HIGH/MEDIUM |
| `/flutter-review` | Flutter | управление состоянием, , производительность | CRITICAL/HIGH/MEDIUM |
| `/fastapi-review` | Python | Pydantic, DI, , Безопасность | CRITICAL/HIGH/MEDIUM |
