# Исправление сборки

 ECC Используется дляошибка. 

---

## /go-build

**НазначениеОписание**:  Go ошибка, go vet Предупреждение linter проблема. 

**Способ использования**:
```
/go-build
```

**Сценарий использования**:
- `go build ./...` неудача
- `go vet ./...` проблема
- `golangci-lint run` Предупреждение
- 
- неудача

****:
1. **** -  `go build`, `go vet`, `staticcheck`
2. **ошибка** - 
3. **** - ошибка
4. **Проверка** - Запустить сборку
5. **** - проблема

**Типичные ошибки**:

| ошибка |  |
|------|----------|
| `не определён: X` |  |
| `cannot use X as Y` | Тип |
| `missing return` |  return  |
| `X does not implement Y` |  |
| `import cycle` |  |
| `declared but not used` |  |
| `cannot find package` | `go get`  `go mod tidy` |

****:
```bash
go build ./...                # проверка
go vet ./...                  # 
staticcheck ./...             #  lint ()
golangci-lint run             # Linting
go mod verify                # Проверка
go mod tidy -v               # 
```

---

## /kotlin-build

**НазначениеОписание**:  Kotlin/Gradle ошибка, Предупреждениепроблема. 

**Способ использования**:
```
/kotlin-build
```

**Сценарий использования**:
- `./gradlew build` неудача
- Kotlin ошибка
- `./gradlew detekt` 
- Gradle неудача
- неудача

**Типичные ошибки**:

| ошибка |  |
|------|----------|
| `Unresolved reference: X` |  |
| `Type mismatch` | Тип |
| `'when' must be exhaustive` |  |
| `Suspend function can only be called from coroutine` |  `suspend`  |
| `Smart cast impossible` |  `val`  `let` |
| `Could not resolve dependency` |  |

****:
```bash
./gradlew build 2>&1                      # проверка
./gradlew detekt 2>&1                      #  ()
./gradlew ktlintCheck 2>&1                # проверка ()
./gradlew dependencies --configuration runtimeClasspath | head -100  # проблема
./gradlew build --refresh-dependencies     #  ()
```

---

## /rust-build

**НазначениеОписание**:  Rust ошибка, проверкапроблемапроблема. 

**Способ использования**:
```
/rust-build
```

**Сценарий использования**:
- `cargo build`  `cargo check` неудача
- `cargo clippy` Предупреждение
- проверкаошибка
- Cargo неудача
- неудача

**Типичные ошибки**:

| ошибка |  |
|------|----------|
| `cannot borrow as mutable` |  |
| `does not live long enough` | Тип |
| `cannot move out of` | владение； |
| `mismatched types` |  `.into()`, `as`  |
| `trait X not implemented` |  `#[derive(Trait)]` Реализовать |
| `unresolved import` |  Cargo.toml  `use`  |
| `cannot find value` |  |

****:
```bash
cargo check 2>&1                               # проверка
cargo clippy -- -D warnings 2>&1               # Lints
cargo fmt --check 2>&1                         # проверка
cargo tree --duplicates                        # 
cargo audit                                   # Безопасность ()
```

---

## /cpp-build

**НазначениеОписание**:  C++ ошибка, CMake проблемапроблема. 

**Способ использования**:
```
/cpp-build
```

**Сценарий использования**:
- `cmake --build build` неудача
- ошибка (, )
- Шаблоннеудача
- /проблема
- неудача

**Типичные ошибки**:

| ошибка |  |
|------|----------|
| `undeclared identifier` |  `#include`  |
| `no matching function` | Тип |
| `не определён reference` | Реализовать |
| `multiple definition` |  `inline`  .cpp |
| `incomplete type` |  `#include`  |
| `no member named X` | Название include |
| `cannot convert X to Y` |  |
| `CMake Error` |  CMakeLists.txt  |

****:
```bash
cmake -B build -S .                        # CMake 
cmake --build build 2>&1 | head -100       # 
clang-tidy src/*.cpp -- -std=c++17         #  ()
cppcheck --enable=all src/                 #  ()
```

---

## /gradle-build

**НазначениеОписание**:  Android  Kotlin Multiplatform (KMP)  Gradle ошибка. 

**Способ использования**:
```
/gradle-build
```

**Сценарий использования**:
- Android неудача
- KMP ошибка
- Gradle неудача
- 

**Типобнаружение**:

|  |  |
|--------|----------|
| `build.gradle.kts` + `composeApp/` (KMP) | `./gradlew composeApp:compileKotlinMetadata` |
| `build.gradle.kts` + `app/` (Android) | `./gradlew app:compileDebugKotlin` |
| `settings.gradle.kts`  | `./gradlew assemble` |
|  detekt | `./gradlew detekt` |

**Типичные ошибки**:

| ошибка |  |
|------|------|
| `commonMain`  | проверка `commonMain.dependencies {}`  |
| Expect  actual |  `actual` Реализовать |
| Compose  |  `libs.versions.toml`  Kotlin  Compose  |
|  |  `./gradlew dependencies` проверка |
| KSP ошибка |  `./gradlew kspCommonMainKotlinMetadata`  |
| проблема | проверкаЗадача |

---

## /flutter-build

**НазначениеОписание**:  Dart ошибка Flutter неудача. 

**Способ использования**:
```
/flutter-build
```

**Сценарий использования**:
- `flutter analyze` ошибка
- `flutter build` неудача
- `flutter pub get` 
- `build_runner` Сгенерировать коднеудача
- неудача

**Типичные ошибки**:

| ошибка |  |
|------|----------|
| `A value of type 'X?' can't be assigned to 'X'` |  `?? default`  |
| `The name 'X' isn't defined` |  |
| `Non-nullable instance field must be initialized` |  `late` |
| `Version solving failed` |  pubspec.yaml  |
| `Missing concrete implementation of 'X'` | Реализовать |
| `build_runner: Part of X expected` |  `.g.dart`  |

****:
```bash
flutter analyze 2>&1                  # 
flutter pub get 2>&1                  # 
dart run build_runner build --delete-conflicting-outputs 2>&1  #  ( build_runner)
flutter build apk 2>&1                # 
flutter build web 2>&1                # Web 
```

---

## Исправление сборки

|  | / |  | проблема |
|------|----------|----------|----------|
| `/go-build` | Go | go build, go vet | Типошибка,  |
| `/kotlin-build` | Kotlin | Gradle, detekt | Тип, when  |
| `/rust-build` | Rust | cargo check, clippy | ошибка,  |
| `/cpp-build` | C++ | CMake, clang-tidy | ошибка, Шаблонпроблема |
| `/gradle-build` | Android/KMP | Gradle | , ошибка |
| `/flutter-build` | Flutter/Dart | Flutter analyze | Безопасность, ошибка |

---

## общий специалистСтратегия исправлений

Исправление сборки：

1. **Исправление ошибок сборки** - Обязательно
2. ** lint Предупреждение** - 
3. **Предупреждение** - Лучшие практики
4. **** - Проверка
5. **** - , 

****:
, ：
- ошибка 3 
- ошибка
- Требуется
- 
