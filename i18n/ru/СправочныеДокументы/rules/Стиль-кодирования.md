# Стиль кода

## Обзор

ECC Rules Стиль кодаЛучшие практики. , поддерживаемость, соглашения. 

## 

###  ()

**Обязательно**：, . 

```typescript
// ошибкаПример - 
function modify(user, name) {
  user.name = name;  // 
  return user;
}

// Пример - Назад
function update(user, name) {
  return { ...user, name };  // 
}
```

**Обоснование**：, , ПоддерживатьБезопасностьпараллелизм. 

### Спроектировать

|  | Описание |  |
|------|------|------|
| **KISS** | Keep It Simple | , ,  |
| **DRY** | Don't Repeat Yourself | , Реализовать |
| **YAGNI** | You Aren't Gonna Need It | Требуется, Начать, Требуется |
### 

|  | Описание |
|------|------|
|  >  | ,  |
|  | 200-400, 800 |
|  |  |
|  | функция/, Тип |

### обработка ошибок

|  | Описание |
|------|------|
|  | ошибка |
| UI | Предоставлятьошибка |
|  | ошибка |
|  | ошибка |

### Проверка

|  | Описание |
|------|------|
| Проверка | Проверка |
| SchemaПроверка | SchemaПроверка |
| неудача | ошибканеудача |
|  |  (API, , ) |
## 

### соглашения

| Тип | соглашения | Пример |
|------|------|------|
|  | camelCase + ОписаниеНазвание | `calculateTotal`, `userName` |
|  | is/has/should/can  | `isActive`, `hasPermission` |
| , Тип,  | PascalCase | `UserProfile`, `ButtonComponent` |
|  | UPPER_SNAKE_CASE | `MAX_RETRY_COUNT`, `API_TIMEOUT` |
| Hooks | camelCase + use | `useUserData`, `useFetch` |

### Код с запашком

#### 

4Назад：

```typescript
// 
function processOrder(order) {
  if (order) {
    if (order.items) {
      if (order.items.length > 0) {
        // 
      }
    }
  }
}

// рекомендации
function processOrder(order) {
  if (!order || !order.items || order.items.length === 0) {
    return;
  }
  // 
}
```

#### 

：

```typescript
// 
setTimeout(callback, 300000);

// рекомендации
const SESSION_TIMEOUT_MS = 5 * 60 * 1000; // 5
setTimeout(callback, SESSION_TIMEOUT_MS);
```

#### 

Ответственность：

|  |  |
|----------|------|
| <50 |  |
| 50-100 | ,  |
| >100 | Обязательно |

## 

### качество кодапроверка

ЗавершитьОбязательнопроверка：

- [ ] 
- [ ]  (<50)
- [ ]  (<800)
- [ ]  (>4)
- [ ] обработка ошибок
- [ ]  ()
- [ ]  (mutation)

### 

|  |  |  |
|------|------|------|
| CRITICAL | Уязвимости безопасности | **** - Обязательно |
| HIGH | Bugпроблема | **Предупреждение** -  |
| MEDIUM | поддерживаемостьпроблема | **Подсказка** -  |
| LOW |  | **** -  |

## 

|  | Описание |
|----------|------|
| Правила тестирования | Тестирование |
| Правила безопасности | Безопасностьпроверка |
| Код-ревью |  |
|  | TDDКод-ревью |
