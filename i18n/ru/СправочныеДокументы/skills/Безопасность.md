# Безопасность

Безопасность, Сканирование уязвимостейФреймворкиБезопасностьЛучшие практики. 

---

## Безопасность

### НазначениеОписание
БезопасностьБезопасностьЛучшие практики, . 

### Когда использовать
- РеализоватьПроверка
- 
- API
- 
- Реализоватьфункция
- 
- API

### Основные концепции

**1. Управлять**
. 

**2. Проверка**
ОбязательноПроверка. 

**3. SQLЗащита от инъекций**
Использовать параметризованные запросы. 

**4. XSS**
ПредоставлятьHTML. 

**5. CSRF**
CSRF. 

**6. **
API. 

### Безопасностьпроверка

| проверка | Описание |
|--------|------|
|  | ,  |
| Проверка | schemaПроверка |
| SQL-инъекция | Использовать параметризованные запросы |
| XSS |  |
| CSRF |  |
| Проверка |  |
|  | Рольпроверка |
|  |  |
| HTTPS |  |
| Безопасность | CSP, X-Frame-Options |
| обработка ошибок | ошибка |
|  |  |
|  | ,  |

### Пример

```typescript
// Управлять - ошибка
const apiKey = "sk-proj-xxxxx"  // 

// Управлять - 
const apiKey = process.env.OPENAI_API_KEY
if (!apiKey) {
  throw new Error('OPENAI_API_KEY not configured')
}

// Проверка
import { z } from 'zod'

const CreateUserSchema = z.object({
  email: z.string().email(),
  name: z.string().min(1).max(100),
})

// SQLЗащита от инъекций - ошибка
const query = `SELECT * FROM users WHERE email = '${userEmail}'`  // ！

// SQLЗащита от инъекций - 
const { data } = await supabase
  .from('users')
  .select('*')
  .eq('email', userEmail)

// XSS
import DOMPurify from 'isomorphic-dompurify'

function renderUserContent(html: string) {
  const clean = DOMPurify.sanitize(html, {
    ALLOWED_TAGS: ['b', 'i', 'em', 'strong', 'p'],
    ALLOWED_ATTR: []
  })
  return <div dangerouslySetInnerHTML={{ __html: clean }} />
}
```

---

## Безопасность

### НазначениеОписание
AgentShieldClaude Code (`.claude/`Содержание)Уязвимости безопасности, ошибка. 

### Когда использовать
- Claude Code
- `.claude/settings.json`, `CLAUDE.md`MCP
- 
- Claude Code
- Безопасностьпроверка

### 

|  | проверка |
|------|--------|
| `CLAUDE.md` | , , Подсказка |
| `settings.json` | , ,  |
| `mcp.json` | MCP, env, npx |
| `hooks/` | Через, , ошибка |
| `agents/*.md` | , Подсказка,  |

### Пример

```bash
# 
npx ecc-agentshield scan

# 
npx ecc-agentshield scan --path /path/to/.claude

# 
npx ecc-agentshield scan --min-severity medium

# JSON (Используется дляCI/CD)
npx ecc-agentshield scan --format json

# 
npx ecc-agentshield scan --fix

# Opus (ТребуетсяANTHROPIC_API_KEY)
export ANTHROPIC_API_KEY=your-key
npx ecc-agentshield scan --opus --stream
```

### 

|  |  |  |
|------|------|------|
| A | 90-100 | Безопасность |
| B | 75-89 | проблема |
| C | 60-74 | Требуется |
| D | 40-59 |  |
| F | 0-39 |  |

---

## DjangoБезопасность

### НазначениеОписание
DjangoБезопасностьЛучшие практики, . 

### Когда использовать
- DjangoПроверка
- РеализоватьРоль
- Безопасность
- DjangoБезопасностьпроблема
- Django

### Безопасность

```python
# settings/production.py
DEBUG = False  # ：True

ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', '').split(',')

# Безопасность
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
SECURE_HSTS_SECONDS = 31536000  # 1
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = True
SECURE_CONTENT_TYPE_NOSNIFF = True
SECURE_BROWSER_XSS_FILTER = True
X_FRAME_OPTIONS = 'DENY'

# CookieБезопасность
SESSION_COOKIE_HTTPONLY = True
CSRF_COOKIE_HTTPONLY = True
SESSION_COOKIE_SAMESITE = 'Lax'

# Проверка
AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator', 'OPTIONS': {'min_length': 12}},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
]
```

### SQLЗащита от инъекций

```python
# Безопасность：Django ORM
def get_user(username):
    return User.objects.get(username=username)  # Безопасность

# Безопасность：raw()
def search_users(query):
    return User.objects.raw('SELECT * FROM users WHERE username = %s', [query])

# ：
def get_user_bad(username):
    return User.objects.raw(f'SELECT * FROM users WHERE username = {username}')  # ！
```

### XSS

```django
{# Django - Безопасность #}
{{ user_input }}  {# HTML #}

{# БезопасностьИспользуется для #}
{{ trusted_html|safe }}  {#  #}

{# ШаблонБезопасностьHTML #}
{{ user_input|striptags }}  {# HTML #}
```

### CSRF

```django
{# Шаблон #}
<form method="post">
    {% csrf_token %}
    {{ form.as_p }}
    <button type="submit"></button>
</form>

{# AJAX #}
function getCookie(name) {
    let cookieValue = null;
    if (document.cookie && document.cookie !== '') {
        const cookies = document.cookie.split(';');
        for (let i = 0; i < cookies.length; i++) {
            const cookie = cookies[i].trim();
            if (cookie.substring(0, name.length + 1) === (name + '=')) {
                cookieValue = decodeURIComponent(cookie.substring(name.length + 1));
                break;
            }
        }
    }
    return cookieValue;
}

fetch('/api/endpoint/', {
    method: 'POST',
    headers: {
        'X-CSRFToken': getCookie('csrftoken'),
        'Content-Type': 'application/json',
    },
    body: JSON.stringify(data)
});
```

### 

```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_THROTTLE_CLASSES': [
        'rest_framework.throttling.AnonRateThrottle',
        'rest_framework.throttling.UserRateThrottle'
    ],
    'DEFAULT_THROTTLE_RATES': {
        'anon': '100/day',
        'user': '1000/day',
    }
}
```

### Безопасностьпроверка

| проверка | Описание |
|--------|------|
| `DEBUG = False` | DEBUG |
| HTTPS | SSL, Безопасностьcookie |
|  | SECRET_KEY |
| Проверка | Проверка |
| CSRF | ,  |
| XSS | Django, `\|safe` |
| SQL-инъекция | ORM,  |
|  | ПроверкаТип |
|  | API |
| Безопасность | CSP, X-Frame-Options, HSTS |
|  | Безопасность |
|  | Django |

---

## 

|  | Назначение |
|------|------|
| `security-review` | Безопасностьпроверка |
| `security-scan` | Claude Code |
| `django-security` | DjangoБезопасностьЛучшие практики |
| `coding-standards` | Безопасность |
