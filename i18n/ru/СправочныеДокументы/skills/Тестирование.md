# Тестирование

本部分涵盖各Языки программирования的Тестирование策略、TDD工作流和E2EТестирование模式。

---

## TDD工作流

### НазначениеОписание
TDD工作流确保所有代码开发遵循Разработка через тестирование原则，具有全面的Тестирование覆盖率。

### Когда использовать
- 编写新功能或功能
- 修复bug或问题
- 重构现有代码
- 添加API端点
- 创建新组件

### Основные концепции

**1. Тестирование先行**
总是先写Тестирование，然后Реализовать代码使ТестированиеЧерез。

**2. 覆盖率要求**
最低80%覆盖率（单元Тестирование + 集成Тестирование + E2EТестирование）

**3. Тестирование型**
- 单元Тестирование：独立函数和组件
- 集成Тестирование：API端点和数据库操作
- E2EТестирование：关键用户流和浏览器自动化

**4. RED-GREEN-REFACTOR循环**
- RED: 先写失败的Тестирование
- GREEN: 写最简代码使ТестированиеЧерез
- REFACTOR: 保持Тестирование绿色的情况下改进代码

### TDD流程Шаги

```bash
# 1. 写Тестирование（RED）
npm test  # ТестированиеСледует失败

# 2. Реализовать代码（GREEN）
# 写最简代码使ТестированиеЧерез

# 3. 重构（REFACTOR）
# 改进Качество кода，保持Тестирование绿色

# 4. Проверка覆盖率
npm run test:coverage  # Проверка80%+覆盖率
```

### Тестирование隔离原则

```typescript
// 错误：Тестирование相互依赖
test('creates user', () => { /* ... */ })
test('updates same user', () => { /* 依赖前一个Тестирование */ })

// 正确：每个Тестирование独立设置
test('creates user', () => {
  const user = createTestUser()
  // Тестирование逻辑
})

test('updates user', () => {
  const user = createTestUser()
  // 更新逻辑
})
```

---

## PythonТестирование

### НазначениеОписание
PythonТестирование策略使用pytest、TDD方法论、fixtures、mocking、参数化和覆盖率要求。

### Когда использовать
- 遵循TDD方法论编写新Python代码
- СпроектироватьPython项目Тестирование套件
- 审查PythonТестирование覆盖率
- 设置Тестирование基础设施

### Основные концепции

**1. pytest Fixtures**
使用`@pytest.fixture`创建可复用Тестирование数据。

**2. 参数化Тестирование**
使用`@pytest.mark.parametrize`用不同输入运行Тестирование。

**3. Mocking**
使用`unittest.mock`的`@patch`模拟外部依赖。

**4. 覆盖率**
使用`--cov`测量覆盖率，Цель80%+。

### 使用Пример

```python
import pytest
from unittest.mock import patch

# Fixtures
@pytest.fixture
def database():
    db = Database(":memory:")
    db.create_tables()
    yield db
    db.close()

# 参数化Тестирование
@pytest.mark.parametrize("input,expected", [
    ("hello", "HELLO"),
    ("world", "WORLD"),
    ("PyThOn", "PYTHON"),
])
def test_uppercase(input, expected):
    assert input.upper() == expected

# Mocking
@patch("mypackage.external_api_call")
def test_with_mock(api_call_mock):
    api_call_mock.return_value = {"status": "success"}
    result = my_function()
    assert result["status"] == "success"

# 异常Тестирование
with pytest.raises(ValueError, match="invalid input"):
    raise ValueError("invalid input provided")

# 运行命令
pytest --cov=mypackage --cov-report=html
```

---

## GoТестирование

### НазначениеОписание
GoТестирование模式Включать表驱动Тестирование、子Тестирование、基准Тестирование、模糊Тестирование和Тестирование覆盖率，遵循TDD方法和惯用Go实践。

### Когда использовать
- 编写新的Go函数或方法
- 为现有代码添加Тестирование覆盖率
- 为性能关键代码创建基准Тестирование
- Реализовать模糊Тестирование进行输入Проверка
- 在Go项目中遵循TDD工作流

### Основные концепции

**1. 表驱动Тестирование**
使用表驱动模式Реализовать全面覆盖。

**2. 子Тестирование**
使用`t.Run`组织相关Тестирование。

**3. Тестирование辅助函数**
使用`t.Helper()`标记辅助函数。

**4. 接口Mocking**
使用接口进行依赖解耦和Тестирование。

**5. 基准Тестирование和模糊Тестирование**
性能Тестирование和随机输入Тестирование。

### 使用Пример

```go
// 表驱动Тестирование
func TestAdd(t *testing.T) {
    tests := []struct {
        name     string
        a, b     int
        expected int
    }{
        {"positive numbers", 2, 3, 5},
        {"negative numbers", -1, -2, -3},
        {"zero values", 0, 0, 0},
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            got := Add(tt.a, tt.b)
            if got != tt.expected {
                t.Errorf("Add(%d, %d) = %d; want %d", tt.a, tt.b, got, tt.expected)
            }
        })
    }
}

// Тестирование辅助函数
func setupTestDB(t *testing.T) *sql.DB {
    t.Helper()
    db, err := sql.Open("sqlite3", ":memory:")
    if err != nil {
        t.Fatalf("failed to open database: %v", err)
    }
    t.Cleanup(func() { db.Close() })
    return db
}

// 接口Mocking
type UserRepository interface {
    GetUser(id string) (*User, error)
    SaveUser(user *User) error
}

type MockUserRepository struct {
    GetUserFunc  func(id string) (*User, error)
    SaveUserFunc func(user *User) error
}

func (m *MockUserRepository) GetUser(id string) (*User, error) {
    return m.GetUserFunc(id)
}

// 基准Тестирование
func BenchmarkProcess(b *testing.B) {
    data := generateTestData(1000)
    b.ResetTimer()
    for i := 0; i < b.N; i++ {
        Process(data)
    }
}

// 运行命令
go test -cover ./...
go test -bench=. -benchmem
go test -fuzz=FuzzParse -fuzztime=30s
```

---

## RustТестирование

### НазначениеОписание
RustТестирование模式Включать单元Тестирование、集成Тестирование、异步Тестирование、属性Тестирование、mocking和覆盖率，遵循TDD方法论。

### Когда использовать
- 编写新的Rust函数、方法或trait
- 为现有代码添加Тестирование覆盖率
- 为性能关键代码创建基准Тестирование
- Реализовать属性Тестирование进行输入Проверка
- 在Rust项目中遵循TDD工作流

### Основные концепции

**1. 模块级Тестирование**
在`#[cfg(test)]`模块中编写单元Тестирование。

**2. 集成Тестирование**
在`tests/`Содержание中编写集成Тестирование。

**3. 异步Тестирование**
使用`#[tokio::test]`进行异步Тестирование。

**4. 属性Тестирование**
使用`proptest`进行基于属性的Тестирование。

**5. Mocking**
使用`mockall`模拟trait。

### 使用Пример

```rust
// 模块级Тестирование
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn creates_user_with_valid_email() {
        let user = User::new("Alice", "alice@example.com").unwrap();
        assert_eq!(user.display_name(), "Alice");
    }

    #[test]
    fn rejects_invalid_email() {
        let result = User::new("Bob", "not-an-email");
        assert!(result.is_err());
    }
}

// 异步Тестирование
#[tokio::test]
async fn fetches_data_successfully() {
    let client = TestClient::new().await;
    let result = client.get("/data").await;
    assert!(result.is_ok());
}

// 属性Тестирование
proptest! {
    #[test]
    fn encode_decode_roundtrip(input in ".*") {
        let encoded = encode(&input);
        let decoded = decode(&encoded).unwrap();
        prop_assert_eq!(input, decoded);
    }
}

// Mocking with mockall
use mockall::{automock, predicate::eq};

#[automock]
trait UserRepository {
    fn find_by_id(&self, id: u64) -> Option<User>;
    fn save(&self, user: &User) -> Result<(), StorageError>;
}

#[test]
fn service_returns_user_when_found() {
    let mut mock = MockUserRepository::new();
    mock.expect_find_by_id()
        .with(eq(42))
        .times(1)
        .returning(|_| Some(User { id: 42, name: "Alice".into() }));

    let service = UserService::new(Box::new(mock));
    let user = service.get_user(42).unwrap();
    assert_eq!(user.name, "Alice");
}

// 运行命令
cargo test
cargo llvm-cov --fail-under-lines 80
cargo bench
```

---

## E2EТестирование

### НазначениеОписание
Playwright E2EТестирование模式Предоставлять了构建稳定、快速、可维护E2EТестирование套件的全面模式，Включать页面对象模型、配置、CI/CD集成和防抖Тестирование策略。

### Когда использовать
- 创建E2E тестированиеПроверка完整用户流
- 使用Playwright进行浏览器自动化
- 设置CI/CD中的E2EТестирование
- 处理不稳定Тестирование

### Основные концепции

**1. 页面对象模型(POM)**
将页面交互封装在页面对象类中。

**2. Тестирование隔离**
每个Тестирование独立设置数据。

**3. 定位器策略**
使用语义定位器而非CSS类名。

**4. 防抖Тестирование**
识别和处理不稳定Тестирование。

**5. ArtifactsУправлять**
截图、视频、跟踪记录Используется для调试。

### 使用Пример

```typescript
import { test, expect } from '@playwright/test'

// 页面对象模型
export class ItemsPage {
  readonly page: Page
  readonly searchInput: Locator
  readonly itemCards: Locator

  constructor(page: Page) {
    this.page = page
    this.searchInput = page.locator('[data-testid="search-input"]')
    this.itemCards = page.locator('[data-testid="item-card"]')
  }

  async goto() {
    await this.page.goto('/items')
    await this.page.waitForLoadState('networkidle')
  }

  async search(query: string) {
    await this.searchInput.fill(query)
    await this.page.waitForResponse(resp => resp.url().includes('/api/search'))
  }
}

// Тестирование结构
test.describe('Item Search', () => {
  let itemsPage: ItemsPage

  test.beforeEach(async ({ page }) => {
    itemsPage = new ItemsPage(page)
    await itemsPage.goto()
  })

  test('should search by keyword', async ({ page }) => {
    await itemsPage.search('test')
    const count = await itemsPage.getItemCount()
    expect(count).toBeGreaterThan(0)
    await expect(itemsPage.itemCards.first()).toContainText(/test/i)
  })
})

// Playwright配置
export default defineConfig({
  testDir: './tests/e2e',
  retries: process.env.CI ? 2 : 0,
  use: {
    baseURL: process.env.BASE_URL || 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
  ],
})

// 防抖Тестирование
test('conditional skip', async ({ page }) => {
  test.skip(process.env.CI, 'Flaky in CI - Issue #123')
})

test('flaky: complex search', async ({ page }) => {
  test.fixme(true, 'Known flaky test - Issue #123')
})
```

---

## 相关技能

| 技能 | Назначение |
|------|------|
| `coding-standards` | 编码标准和Лучшие практики |
| `error-handling` | Обработка ошибок模式 |
| `security-review` | Безопасность审查检查清单 |
