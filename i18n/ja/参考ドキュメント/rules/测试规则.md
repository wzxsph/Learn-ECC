# テストルール

## ルール概要

ECC Rules テストルール定义了确保コード品質的强制性テスト標準。该ルール涵盖テスト驱动開発(TDD)、最低カバレッジ要件和テスト構造仕様，确保すべてコード都经过充分検証。

## コア要件

### 最低テストカバレッジ：80%

mustパッケージ含以下三种テスト類型：

| テスト類型 | 説明 | 覆盖范围 |
|----------|------|----------|
| ユニットテスト | 独立関数、ツールクラス、コンポーネント | Individual units |
| 統合テスト | API端点、データベース操作 | Integration points |
| E2Eテスト | 重要ユーザーフロー | Critical user flows |

### テスト驱动開発（TDD）

**强制ワークフロー**：

```
1. 编写テスト (RED)   - 先写一个会失敗的テスト
2. 実行テスト          - 検証テスト失敗
3. 编写最小実装 (GREEN) - 编写通過テスト的最小コード
4. 実行テスト          - 検証テスト通過
5. リファクタリング (IMPROVE)    - 改善コード構造
6. 検証カバレッジ        - 确保达到80%+
```

## 实施细节

### AAAパターン（Arrange-Act-Assert）

すべてテストmust遵循AAA構造：

```typescript
test('正确计算余弦相似度', () => {
  // Arrange - 准备テストデータ
  const vector1 = [1, 0, 0];
  const vector2 = [0, 1, 0];

  // Act - 実行被テスト的操作
  const similarity = calculateCosineSimilarity(vector1, vector2);

  // Assert - 検証结果
  expect(similarity).toBe(0);
});
```

### テスト命名仕様

使用描述性名称説明被テスト的行为：

```typescript
// 推奨 - 描述预期行为
test('当ない市场匹配查询时戻る空数组', () => {});
test('当API密钥缺失时抛出エラー', () => {});
test('当Redis不可用时回退到子字符串搜索', () => {});

// 避免 - 模糊命名
test('test1', () => {});
test('edge case', () => {});
```

### テスト故障排除

当テスト失敗时：

| ステップ | 操作 |
|------|------|
| 1 | 使用 **tdd-guide** agent |
| 2 | 検査テスト隔离 |
| 3 | 検証Mock正确性 |
| 4 | 修正実装，而非テスト（除非テスト本身あり误） |

### Agentサポート

| Agent | 用途 | 使用タイミング |
|-------|------|----------|
| **tdd-guide** | テスト驱动開発指导 | 新機能開発、エラー修正时强制使用 |
| **code-reviewer** | コードレビュー | コード编写完了后 |

## 违规处理

### カバレッジ不足

- カバレッジ低于80%的コード不得マージ
- CI/CD流水线应阻止低カバレッジコードコミット
- 使用 `c8` 或クラス似ツール生成カバレッジレポート

### テスト失敗处理

```
1. 分析失敗原因
2. 診断は実装問題还はテスト問題
3. 如果は実装問題 - 修正実装コード
4. 如果はテスト問題 - 修正テストコード
5. 确保すべてテスト通過后再コミット
```

### 常见問題

| 問題 | 原因 | 解決方案 |
|------|------|----------|
| Flakyテスト | 非同期操作、竞态条件 | 增加等待时间、使用mock |
| テスト隔离失敗 | 共享状态污染 | 各テスト前リセット状态 |
| Mock不正确 | 期望值与实际不符 | 検証mock設定は否正确 |

## 相关ルール

| 关联ルール | 説明 |
|----------|------|
| コーディングスタイルルール | コード可读性和保守性 |
| セキュリティルール | セキュリティテスト要件 |
| コードレビュールール | テストカバレッジ検査 |
| 開発ワークフロー | TDD流程統合 |
